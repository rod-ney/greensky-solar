import type { StaticImageData } from "next/image";
import appIconAsset from "@/app/icon.png";

/** Shell + auth splits — bundled from `src/app/icon.png` */
export const APP_ICON: StaticImageData = appIconAsset;
